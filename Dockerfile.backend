FROM node:20-alpine

WORKDIR /app

# 复制package文件
COPY backend/package*.json ./

# 安装依赖
RUN npm install

# 复制源代码
COPY backend/ ./

# 创建数据目录
RUN mkdir -p /app/data

# 暴露端口
EXPOSE 8787

# 启动应用
CMD ["npm", "start"]
